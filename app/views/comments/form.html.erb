<%= form_with(module: [ @article, @article.comments.build ], local: ture) do |form|%>
    <p>
    <%= form.label :commenter %><br>
    <%= form.text_field :commenter %>
    </p>
    <p>
    <%= form.label :body %><br>
    <%= form.text_area :body%>
    </p>
    <p>
    <%= form.f.submit %>
    </p>
<% end %>